const getFormItem = state => {
  return state.formItem;
};

const getInitialSelection = state => {
  return state.initialSelection;
};

const getPackshotData = state => {
  return state.packshotData;
};

export default {
  getFormItem,
  getInitialSelection,
  getPackshotData
};
