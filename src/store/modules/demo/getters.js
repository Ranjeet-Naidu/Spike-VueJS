const counter = state => {
  return state.counter;
};

const posts = state => {
  return state.posts;
};

const photos = state => {
  return state.photos;
};

export default {
  counter,
  posts,
  photos
};
